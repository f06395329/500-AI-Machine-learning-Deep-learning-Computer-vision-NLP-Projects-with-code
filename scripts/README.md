This folder contains helper scripts for the repository.

extract_links.py
- Parses the top-level `README.md` to extract the project table entries and writes `projects.csv` and `projects.json` into this folder.

Run:

```powershell
python scripts\extract_links.py
```
